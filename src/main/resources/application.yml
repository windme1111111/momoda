server:
  port: 8091 #修改tomcat端口号

spring:
  datasource:
    #数据库驱动
    driver-class-name: com.mysql.cj.jdbc.Driver
    #数据库连接地址,suessl=false 禁用ssl改用 name/psw serverTimezone市区
    url: jdbc:mysql://localhost:3306/mall?useSSL=false&serverTimezone=Asia/Shanghai

    username: root

    password: zxcvbnm040710

