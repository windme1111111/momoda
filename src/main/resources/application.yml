server:
  port: 8091 #修改tomcat端口号

spring:
  datasource:
    #数据库驱动
    driver-class-name: com.mysql.cj.jdbc.Driver
    #数据库连接地址,suessl=false 禁用ssl改用 name/psw serverTimezone市区
    url: jdbc:mysql://localhost:3306/cent?useSSL=false&serverTimezone=Asia/Shanghai

    username: root

    password: zxcvbnm040710
#  web:
#    resources:
#      static-locations:file:/D/upload/

  # classpath：/static/,




springdoc:
 api-docs:
  enabled: true # 开启Spring文档
 swagger-ui:
  enabled: true
# 请求的格式
default-consumes-media-type: application/json
# 响应的格式
default-produces-media-type: application/json

